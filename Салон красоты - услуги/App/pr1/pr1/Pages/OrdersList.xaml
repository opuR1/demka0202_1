<Page x:Class="pr1.Pages.OrdersList"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:pr1.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="OrdersList" Background="{StaticResource MainBk}">
    <Page.Resources>
        <Style x:Key="ProductItemStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource MainBk}"/>
            <Setter Property="BorderBrush" Value="Black"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="3"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="4"/>
        </Style>
    </Page.Resources>
    <Grid>
        <ListBox x:Name="lbOrders" HorizontalContentAlignment="Stretch" ScrollViewer.HorizontalScrollBarVisibility="Hidden">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="250"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <StackPanel Orientation="Horizontal" Margin="5,0,0,0">
                                <TextBlock x:Name="tblServiceName" Text="{Binding Services.Name}"/>
                                <TextBlock Text=" | "/>
                                <TextBlock x:Name="tblClientFullName" Text="{Binding Clients.FullName}"/>
                            </StackPanel>
                            <TextBlock x:Name="tblEmail" Text="{Binding Clients.Email}"/>
                            <TextBlock x:Name="tblPhone" Text="{Binding Clients.Phone}"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1">
                            <StackPanel Orientation="Horizontal" Margin="5,0,0,0">
                                <TextBlock  Text="Дата начала:"/>
                                <TextBlock  Text="{Binding StartDate}"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="5,0,0,0">
                                <TextBlock  Text="Начало через:"/>
                                <TextBlock Text="{Binding TimeLeft}" Foreground="{Binding TimeColor}"/>
                            </StackPanel>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Page>
