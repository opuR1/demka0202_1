<Page x:Class="pr1.Pages.ServiceList"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:pr1.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ServiceList" Background="{StaticResource MainBk}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="45"/>
            <RowDefinition Height="*" />
            <RowDefinition Height="20" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <StackPanel Orientation="Vertical" Width="200" Height="50" Margin="10,0,10,0" VerticalAlignment="Top">
                <TextBlock Text="Поиск:" Height="20" HorizontalAlignment="Left" VerticalAlignment="Top"/>
                <TextBox x:Name="tbSearch" Width="200" Height="20" HorizontalAlignment="Left" TextChanged="tbSearch_TextChanged"/>
            </StackPanel>
            
            <StackPanel Orientation="Vertical" Width="100" Height="50" VerticalAlignment="Top">
                <TextBlock Text="Сортировка" Height="20" HorizontalAlignment="Left" VerticalAlignment="Top"/>
                <ComboBox x:Name="cmbSort" SelectionChanged="cmbSort_SelectionChanged">
                    <ComboBoxItem Content=" Price asc" />
                    <ComboBoxItem Content=" Price desc" />
                </ComboBox>
            </StackPanel>
            <StackPanel Orientation="Vertical" Margin="10,0,10,0" Width="100" Height="50" VerticalAlignment="Top">
                <TextBlock Text="Скидки" Height="20" HorizontalAlignment="Left" VerticalAlignment="Top"/>
                <ComboBox x:Name="cmbFilter" SelectionChanged="cmbFilter_SelectionChanged">
                    <ComboBoxItem Content="Все" />
                    <ComboBoxItem Content="до 5%" />
                    <ComboBoxItem Content="от 5% до 15%" />
                    <ComboBoxItem Content="от 15% до 30%" />
                    <ComboBoxItem Content="от 30% до 70%" />
                    <ComboBoxItem Content="от 70% до 100%" />
                </ComboBox>
            </StackPanel>
            <Button Content="Add" x:Name="btnAdd" Visibility="Collapsed" Width="50" Height="25" Margin="10,0,0,0" Background="{StaticResource AccentBk}" Click="btnAdd_Click" FontWeight="Bold"/>
            <Button Content="Записать клиента" x:Name="btnAddClientService" Visibility="Collapsed" Width="150" Height="25" Margin="10,0,0,0" Background="{StaticResource AccentBk}" FontWeight="Bold" Click="btnAddClientService_Click"/>
            <Button Content="Записи" x:Name="btnServices" Visibility="Collapsed" Width="50" Height="25" Margin="10,0,0,0" Background="{StaticResource AccentBk}" FontWeight="Bold" Click="btnServices_Click"/>
        </StackPanel>
        <ListBox Grid.Row="1" x:Name="lbServices" HorizontalContentAlignment="Stretch" ScrollViewer.HorizontalScrollBarVisibility="Hidden" MouseDoubleClick="lbServices_MouseDoubleClick">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="200"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="200"/>
                        </Grid.ColumnDefinitions>
                        <Image x:Name="imgService" Source="{Binding ImagePath}" Grid.Column="0" Stretch="Uniform" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        <StackPanel Grid.Column="1" Orientation="Vertical" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="10">
                            <TextBlock Text="{Binding Name}" FontSize="14" Margin="5"/>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Продолжительность: " FontSize="14" Margin="5,5,0,5"/>
                                <TextBlock Text="{Binding Duration}" FontSize="14" Margin="0,5,0,5"/>
                                <TextBlock Text=" мин." FontSize="14" Margin="0,5,0,5"/>
                            </StackPanel>
                        </StackPanel>
                        <StackPanel Orientation="Vertical" Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="10">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Стоимость: " FontSize="14" Margin="5,5,0,5"/>
                                <TextBlock Text="{Binding Cost, StringFormat={}{0:N2}}" FontSize="14" Margin="0,5,0,5"/>
                                <TextBlock Text=" руб." FontSize="14" Margin="0,5,0,5"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding checkDiscount}" FontSize="14" Margin="5,5,0,5"/>
                            </StackPanel>
                            <TextBlock Text="{Binding TotalPrice, StringFormat={}{0:N2}}" FontSize="14" Margin="5,5,0,5"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <TextBlock Grid.Row="2" x:Name="txbCount" FontSize="14" HorizontalAlignment="Right"/>
    </Grid>
</Page>
